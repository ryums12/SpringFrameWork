spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/personal?serverTimezone=Asia/Seoul
    username: root
    password: archivsoft

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: mapper/*.xml

server:
  port: 8082
